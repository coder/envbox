spec:
  template:
    metadata:
      annotations:
        experiment.coder.com/selinux-test: super-t
    spec:
      containers:
        - name: prep
          securityContext:
            seLinuxOptions:
              type: super_t
